# Django-Blog-Theme
A Django blog with categories, featured posts, and a custom magazine-style theme.
Django blog with categories, featured/trending posts, and slug-based post pages. Includes admin, image uploads, and static pages (contact, write for us).

Run: `pip install -r requirements.txt` → `python manage.py migrate` → `python manage.py runserver`
